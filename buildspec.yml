version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo "Installing dependencies for validation..."
      - pip install -r requirements.txt
  build:
    commands:
      - echo "Build phase - preparing deployment package"
artifacts:
  files:
    - '**/*'
  name: BuildArtifact